% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-extrapolation.R
\name{check_extrapolation}
\alias{check_extrapolation}
\title{Diagnose extrapolation for binary exposures with Gower distances and the
convex hull}
\usage{
check_extrapolation(
  .data,
  .exposure,
  .covariates,
  nearby = 1,
  hull = NULL,
  exposure_type = c("auto", "binary")
)
}
\arguments{
\item{.data}{A data frame.}

\item{.exposure}{The binary exposure column, selected with data-masking.
\code{check_extrapolation()} aborts unless the exposure has exactly two distinct
values, whether that type is detected or declared through \code{exposure_type}.}

\item{.covariates}{The covariate columns, selected with tidyselect. Numeric
and categorical covariates are both supported by the Gower distance.}

\item{nearby}{The reference radius as a multiple of the geometric variability.
An observation counts an opposite-group unit as nearby when their Gower
distance is at most \code{nearby} times the geometric variability. A single
positive number, defaulting to \code{1}.}

\item{hull}{Whether to run the convex-hull membership test. \code{NULL} (the
default) runs it automatically when there is at least one and at most ten
numeric covariates and \pkg{lpSolve} is installed, and skips it otherwise.
\code{TRUE} forces it to run, aborting when \pkg{lpSolve} is not installed and
warning when there are no numeric covariates or the dimension is high.
\code{FALSE} skips it.}

\item{exposure_type}{One of \code{"auto"} (detect from the data, the default) or
\code{"binary"}. A supplied type is authoritative and detection is not consulted,
so \code{"binary"} is rejected only when the exposure column does not have
exactly two distinct values. Declaring it unlocks no call that \code{"auto"}
would refuse, since detection reads any two-valued column as binary; it is
accepted so that every diagnostic takes the same argument.}
}
\value{
An \code{extrapolation_result} object, an S7 subclass of
\link{positivity_diagnostic}. Its \verb{@results} tibble has one row per observation
with columns \code{.id} (the row index), \code{exposure} (the exposure value),
\code{frac_nearby}, \code{gower_min}, \code{gower_mean}, \code{in_hull} (the hull membership
flag, or \code{NA} when the hull test did not run), and \code{low_support} (\code{TRUE}
when the nearest opposite-group unit lies farther away than one geometric
variability). It also carries the scalar properties
\verb{@geometric_variability} and \verb{@hull_run}, and a per-group \verb{@summary} tibble
with one row per exposure level and columns \code{exposure}, \code{n},
\code{mean_frac_nearby}, \code{mean_gower_min}, \code{prop_supported} (the one geometric
variability fraction described in Details), and \code{prop_in_hull} (the
fraction inside the opposite group's hull, \code{NA} when the hull test did not
run).

\code{\link[generics:glance]{generics::glance()}} returns a one-row tibble with \code{n} (the sample size),
\code{nearby}, \code{geometric_variability}, \code{mean_frac_nearby}, \code{prop_supported},
\code{hull_run} (kept logical), and \code{prop_in_hull}, which is \code{NA} when the hull
test did not run. The two readings, \code{prop_supported} and \code{prop_in_hull},
answer different questions and are both reported.
}
\description{
\code{check_extrapolation()} implements the extrapolation diagnostics of King and
Zeng (2006) for binary point exposures. It measures, for every observation,
how well the opposite exposure group covers its position in covariate space,
using Gower distances that handle mixed numeric and categorical covariates,
and it optionally tests convex-hull membership against the opposite group.
}
\details{
Estimating a causal contrast requires comparing treated and untreated units
with similar covariates. Where one exposure group has no nearby members of the
other, the contrast rests on extrapolation rather than data, the concern
framed by Petersen and colleagues (2012) and by King and Zeng (2006).

The Gower distance between two rows is the mean over covariates of a
per-covariate distance: for a numeric covariate, the absolute difference
divided by the pooled sample range; for a factor or character covariate, one
when the categories differ and zero when they agree. Distances therefore lie
in \verb{[0, 1]} and mixed covariate types combine on a common scale. Because the
denominator is the sample range, a single extreme covariate value rescales
every distance on that axis.

The geometric variability is one half the mean of the full Gower distance
matrix, the Cuadras-Arenas quantity used by the WhatIf software. It sets the
reference radius: an observation is counted as nearby-supported when an
opposite-group unit lies within \code{nearby} geometric variabilities. Three
per-unit statistics are reported: \code{gower_min} (distance to the nearest
opposite-group unit), \code{gower_mean} (mean distance to the opposite group), and
\code{frac_nearby} (the fraction of the opposite group within \code{nearby} geometric
variabilities). A planted support gap raises \code{gower_min} and lowers
\code{frac_nearby} in both directions. The per-group \verb{@summary} column
\code{prop_supported}, and the \code{prop_supported} statistic in \code{\link[=glance]{glance()}} and the
print method, share one fixed definition: the fraction of units whose nearest
opposite-group unit lies within one geometric variability
(\code{gower_min <= geometric_variability}), independent of \code{nearby}. The per-unit
\code{low_support} column is the complement of that condition, so \code{prop_supported}
is the fraction of rows whose \code{low_support} is \code{FALSE}.

When every covariate is constant, all rows coincide, the geometric
variability is zero, and each observation is at distance zero from the
opposite group. Such an observation is fully supported: \code{gower_min} is zero
and \code{frac_nearby} is one. Constant numeric covariates likewise contribute
zero distance on their own axis without affecting the others.

The convex-hull test asks whether an observation lies inside the convex hull
of the opposite group, solved as a linear-programming feasibility problem
(weights that are non-negative, sum to one, and reproduce the point). The hull
test uses the numeric covariates only, since categorical covariates carry no
ordering to interpolate over. In-hull membership collapses toward zero as the
covariate dimension grows even under perfect overlap, so the test is
degenerate in high dimensions (D'Amour and colleagues, 2021). It therefore
runs automatically only when there is at least one and at most ten numeric
covariates and \pkg{lpSolve} is installed. With no numeric covariates, more
than ten of them, or \pkg{lpSolve} absent, it is skipped and \code{in_hull} is
\code{NA}.
Setting \code{hull = TRUE} forces the test to run and warns when the dimension is
high; \code{hull = FALSE} skips it.
}
\examples{
set.seed(1)
n <- 100
x1 <- rnorm(n)
x2 <- rnorm(n)
exposure <- rbinom(n, 1, plogis(0.5 * x1 - 0.5 * x2))
df <- data.frame(exposure = exposure, x1 = x1, x2 = x2)

# hull = FALSE keeps the example free of the optional lpSolve dependency.
result <- check_extrapolation(df, exposure, c(x1, x2), hull = FALSE)
result

}
\references{
King G, Zeng L (2006). The Dangers of Extreme Counterfactuals.
\emph{Political Analysis}, 14(2):131-159. \doi{10.1093/pan/mpj004}

Petersen ML, Porter KE, Gruber S, Wang Y, van der Laan MJ (2012). Diagnosing
and responding to violations in the positivity assumption.
\emph{Statistical Methods in Medical Research}, 21(1):31-54.
\doi{10.1177/0962280210386207}

D'Amour A, Ding P, Feller A, Lei L, Sekhon J (2021). Overlap in Observational
Studies with High-Dimensional Covariates. \emph{Journal of Econometrics},
221(2):644-654. \doi{10.1016/j.jeconom.2019.10.014}
}
