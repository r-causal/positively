% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-positivity-check.R
\name{autoplot.positivity_check}
\alias{autoplot.positivity_check}
\alias{plot.positivity_check}
\title{Plot the diagnostics a positivity check holds}
\arguments{
\item{object}{A \link{positivity_check} from \code{\link[=check_positivity]{check_positivity()}}.}

\item{diagnostic}{Optionally, the name of one diagnostic to draw. Defaults to
\code{NULL}, which panels every child.}

\item{...}{Passed to a child's own \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}} method. The panel
passes them to every child, so an argument only one diagnostic accepts
belongs with a named \code{diagnostic} rather than with the panel.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws each child's default view. With no \code{diagnostic}, the views are composed
into one panel; naming a diagnostic draws that child alone.
}
\details{
The panel needs the \pkg{patchwork} package, which composes the children into
a single object that is still a \link[ggplot2:ggplot]{ggplot2::ggplot} and so can be built on
further. Without it, naming a diagnostic still works, because that path draws
one child's plot and composes nothing.

A diagnostic is named the way \code{\link[=names]{names()}} lists it and \code{$} extracts it, which is
also the name the printed report heads its sections with, so a reader can go
from a section straight to its plot.
}
\examples{
\dontshow{if (rlang::is_installed("patchwork")) withAutoprint(\{ # examplesIf}
set.seed(1)
n <- 300
x1 <- rnorm(n)
df <- data.frame(exposure = rbinom(n, 1, plogis(3 * x1)), x1 = x1)

check <- check_positivity(df, exposure, x1, diagnostics = c("port", "edp"))

autoplot(check)
autoplot(check, "port")
\dontshow{\}) # examplesIf}
}
