% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aaa-classes.R
\name{positivity_diagnostic}
\alias{positivity_diagnostic}
\title{The abstract parent class for positivity diagnostics}
\arguments{
\item{results}{A \link[tibble:tibble]{tibble} of tidy diagnostic output.}

\item{exposure}{The exposure column name or names, time-ordered when the
diagnostic is sequential.}

\item{exposure_type}{One of \code{"binary"}, \code{"categorical"}, or \code{"continuous"}.}

\item{n}{The number of observations, an integer.}

\item{params}{A list of method parameters as supplied and resolved.}

\item{call}{The originating call.}
}
\value{
An abstract class object. Construction of a subclass returns a
\code{positivity_diagnostic}.
}
\description{
\code{positivity_diagnostic} is the abstract S7 parent that every positively
diagnostic result inherits from. It cannot be instantiated directly. It fixes
the shared property set (a tidy results tibble, the exposure column names and
type, the sample size, the resolved method parameters, and the originating
call) and supplies the \code{\link[generics:tidy]{generics::tidy()}}, \code{\link[generics:glance]{generics::glance()}}, and
\code{\link[=print]{print()}} behavior that every diagnostic reuses. Package developers extend
it when adding a new diagnostic.
}
\details{
\code{\link[generics:tidy]{generics::tidy()}} returns \verb{@results} unchanged. The inherited
\code{\link[generics:glance]{generics::glance()}} returns a one-row \link[tibble:tibble]{tibble} holding the
single column \code{n}, the sample size, because a subclass that adds no
statistics of its own has nothing else to report. Each diagnostic overrides
it to state its own statistics beside \code{n}, typed as they are computed.
}
