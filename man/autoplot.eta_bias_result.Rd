% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-eta-bias.R
\name{autoplot.eta_bias_result}
\alias{autoplot.eta_bias_result}
\alias{plot.eta_bias_result}
\title{Plot an ETA.Bias diagnostic}
\arguments{
\item{object}{An \code{eta_bias_result} from \code{\link[=check_eta_bias]{check_eta_bias()}}.}

\item{type}{One of \code{"bootstrap"} or \code{"sweep"}.}

\item{...}{Not used.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws one of two views of a \code{\link[=check_eta_bias]{check_eta_bias()}} result. \code{type = "bootstrap"}
shows the distribution of bootstrap estimates as a histogram with the target
\code{truth} marked, one facet per truncation level. \code{type = "sweep"} shows
ETA.Bias with a two-Monte-Carlo-standard-error band against the truncation
lower bound, the bias-variance tradeoff of weight truncation.
}
\examples{
set.seed(1)
n <- 300
x1 <- rnorm(n)
x2 <- rnorm(n)
a <- rbinom(n, 1, plogis(2 * (x1 + x2)))
y <- a + x1 + x2 + rnorm(n)
df <- data.frame(a = a, y = y, x1 = x1, x2 = x2)
# n_boot is small here to keep the example fast.
result <- check_eta_bias(df, a, y, c(x1, x2), n_boot = 25)
ggplot2::autoplot(result, type = "bootstrap")

swept <- check_eta_bias(
  df,
  a,
  y,
  c(x1, x2),
  truncation_grid = c(0, 0.05, 0.1),
  n_boot = 25
)
ggplot2::autoplot(swept, type = "sweep")

}
