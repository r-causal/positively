% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-hat-values.R
\name{check_hat_values}
\alias{check_hat_values}
\title{Diagnose positivity for continuous exposures with hat values}
\usage{
check_hat_values(
  .data,
  .exposure,
  .covariates,
  probs = seq(0.05, 0.95, by = 0.05),
  threshold = 2,
  null_reps = 500,
  null_method = c("permutation", "bootstrap", "gaussian"),
  conf_level = 0.95,
  exposure_type = c("auto", "continuous")
)
}
\arguments{
\item{.data}{A data frame.}

\item{.exposure}{The continuous exposure column, selected with data-masking.
Under the default \code{exposure_type = "auto"}, \code{check_hat_values()} aborts when
the exposure is detected as binary or categorical; declaring
\code{exposure_type = "continuous"} passes any numeric column through the type
gate.}

\item{.covariates}{The covariate columns, selected with tidyselect.}

\item{probs}{A numeric vector of exposure percentiles, each in \verb{[0, 1]}, at
which candidate points are formed. Defaults to \code{seq(0.05, 0.95, by = 0.05)}.}

\item{threshold}{The leverage cutoff multiplier. A candidate is flagged when
its hat value exceeds \code{threshold * p / n}. Defaults to \code{2}.}

\item{null_reps}{The number of null replicates used to calibrate
\eqn{\hat{\phi}}. Defaults to \code{500}.}

\item{null_method}{The null-resampling scheme, one of \code{"permutation"}
(permute observed exposures, the default), \code{"bootstrap"} (resample observed
exposures with replacement), or \code{"gaussian"} (draw from a matched normal).
A uniform null is deliberately not offered; see Details.}

\item{conf_level}{The null quantile, strictly between 0 and 1, against which the observed
\eqn{\hat{\phi}} is compared. Defaults to \code{0.95}.}

\item{exposure_type}{One of \code{"auto"} (detect from the data, the default) or
\code{"continuous"}. A supplied type is authoritative and detection is not
consulted, so \code{"continuous"} is rejected only when the exposure column is
not numeric. Declaring it on a numeric column with few distinct values, a
dose recorded at a handful of milligram levels for instance, is exactly the
supported use: the unique-value heuristic reads such a column as
categorical, so under \code{"auto"} the call aborts. Because numeric is the whole
of the requirement, a two-valued numeric column is then accepted as well,
which is the price of an authoritative declaration.}
}
\value{
A \code{hat_values_result} object, an S7 subclass of
\link{positivity_diagnostic}. Its \verb{@results} tibble has one row per candidate
point with columns \code{.id} (the covariate row index), \code{prob} (the exposure
percentile), \code{value} (the candidate exposure value \eqn{d_q}), \code{hat_value}
(the leverage), and \code{high_leverage} (the logical flag). It also carries the
scalar properties \verb{@phi_hat}, \verb{@null_dist}, \verb{@null_quantile},
\verb{@exceeds_null}, and \verb{@p}, the number of model parameters
\eqn{p = q + 2} that sets the \eqn{2p/n} leverage cutoff.

\code{\link[generics:glance]{generics::glance()}} returns a one-row tibble with \code{n} (the sample size),
\code{phi_hat}, \code{null_quantile}, \code{exceeds_null} (kept logical), the counts
\code{n_high_leverage} and \code{n_candidates} that \code{phi_hat} is the ratio of, and
\code{p}.
}
\description{
\code{check_hat_values()} computes the hat-value (leverage) positivity diagnostic
of Moodie and Schulz (2025) for continuous exposures. It measures how far a set
of candidate exposure-covariate combinations sits from the observed data by
the leverage those combinations would exert on a linear model, then compares
the observed leverage profile against a null in which the exposure is drawn
independently of the covariates.
}
\details{
The method (the paper's Box 1) fits the linear design matrix
\eqn{M = [1, D, X]}, where \code{D} is the continuous exposure and \code{X} the
covariates, so the number of parameters is \eqn{p = q + 2} for \code{q}
covariates. For each requested exposure percentile \eqn{d_q} and each
observed covariate row \eqn{x_i}, it forms the candidate point
\eqn{x_* = (1, d_q, x_i)} and computes its leverage (hat value)
\eqn{h_* = x_*^\top (M^\top M)^{-1} x_*}. A candidate is flagged as high
leverage when \eqn{h_* > \mathrm{threshold} \times p / n}, with the default
\code{threshold} of 2 reproducing the paper's \eqn{2p/n} rule. The summary
statistic \eqn{\hat{\phi}} is the proportion of candidates that are high
leverage.

Because a high value of \eqn{\hat{\phi}} can arise from the shape of the
exposure distribution alone, the diagnostic calibrates it against a null in
which the exposure is drawn independently of the covariates. Each of
\code{null_reps} replicates redraws the exposure, rebuilds the design matrix, and
recomputes \eqn{\hat{\phi}} at the same candidate points. The observed
\eqn{\hat{\phi}} is compared against the \code{conf_level} quantile of this null
distribution; when it exceeds that quantile, \code{exceeds_null} is \code{TRUE}.

Three null-resampling schemes are available through \code{null_method}.
\code{"permutation"} (the default) permutes the observed exposures, \code{"bootstrap"}
resamples them with replacement, and \code{"gaussian"} draws from a normal
distribution matched to the observed exposure mean and standard deviation.
The permutation and bootstrap schemes are empirical: they preserve the
observed exposure distribution exactly. A uniform null is deliberately not
offered, because a uniform draw discards that distribution: on a skewed
exposure it spreads the draws more evenly than the data are, which inflates
the null quantile and costs power against a real violation.

The per-candidate \code{high_leverage} flag is a conservative leverage indicator
that feeds \eqn{\hat{\phi}}, not a calibrated per-unit accept or reject rule.
The absolute value of \eqn{\hat{\phi}} depends steeply on the candidate
percentile grid, so the null comparison, not the raw magnitude, carries the
diagnostic signal.
}
\examples{
set.seed(1)
n <- 200
x1 <- rnorm(n)
# The exposure depends on x1, so extreme doses are implausible for some
# covariate values.
dose <- rnorm(n, mean = x1)
df <- data.frame(dose = dose, x1 = x1)

# null_reps is small here to keep the example fast.
result <- check_hat_values(df, dose, x1, null_reps = 50)
result

}
\references{
Moodie EEM, Schulz J (2025). A Simple Diagnostic for the Positivity
Assumption for Continuous Exposures. \emph{Statistics in Medicine}, 44:e70194.
\doi{10.1002/sim.70194}
}
