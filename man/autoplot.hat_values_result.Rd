% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-hat-values.R
\name{autoplot.hat_values_result}
\alias{autoplot.hat_values_result}
\alias{plot.hat_values_result}
\title{Plot a hat-value diagnostic}
\arguments{
\item{object}{A \code{hat_values_result} from \code{\link[=check_hat_values]{check_hat_values()}}.}

\item{type}{One of \code{"null"} or \code{"profile"}.}

\item{...}{Not used.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws one of two views of a \code{\link[=check_hat_values]{check_hat_values()}} result. \code{type = "null"}
shows the null distribution of \eqn{\hat{\phi}} as a histogram with the
observed \eqn{\hat{\phi}} and the \code{conf_level} null quantile marked.
\code{type = "profile"} shows the proportion of high-leverage candidates at each
exposure percentile, the leverage profile across the exposure range.
}
\examples{
set.seed(1)
n <- 200
x1 <- rnorm(n)
dose <- rnorm(n, mean = x1)
df <- data.frame(dose = dose, x1 = x1)
# null_reps is small here to keep the example fast.
result <- check_hat_values(df, dose, x1, null_reps = 50)

ggplot2::autoplot(result, type = "null")
ggplot2::autoplot(result, type = "profile")

}
