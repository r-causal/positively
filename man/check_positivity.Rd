% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-positivity.R
\name{check_positivity}
\alias{check_positivity}
\title{Diagnose positivity across the applicable methods}
\usage{
check_positivity(
  .data,
  .exposure,
  .covariates,
  diagnostics = NULL,
  exposure_type = c("auto", "binary", "categorical", "continuous"),
  args = list()
)
}
\arguments{
\item{.data}{A data frame.}

\item{.exposure}{The exposure column, selected with data-masking.}

\item{.covariates}{The covariate columns, selected with tidyselect. Required,
with no default, so that outcome columns are not swept in by accident. The
exposure column must not be selected.}

\item{diagnostics}{The diagnostics to run. \code{NULL} (the default) selects the
applicable set for the exposure type; otherwise a subset of \code{"edp"},
\code{"port"}, \code{"hat_values"}, \code{"hdr"}, and \code{"extrapolation"}, run in the order
given.}

\item{exposure_type}{One of \code{"auto"} (detect from the data, the default),
\code{"binary"}, \code{"categorical"}, or \code{"continuous"}. An explicit value is passed
to \code{\link[=check_edp]{check_edp()}} and \code{\link[=check_port]{check_port()}}, which honor it directly. The three
diagnostics that re-detect the type (\code{\link[=check_extrapolation]{check_extrapolation()}},
\code{\link[=check_hat_values]{check_hat_values()}}, and \code{\link[=check_hdr]{check_hdr()}}) are instead checked up front
against the detected type; if a requested one of them cannot run on the
detected type, the call aborts before any diagnostic runs.}

\item{args}{A named list of per-diagnostic option lists, for example
\code{list(port = list(alpha = 0.1))}. Each name must be a diagnostic being run.}
}
\value{
A \link{positivity_check} object bundling one \link{positivity_diagnostic} child
per diagnostic, aligned with the \verb{@diagnostics} names.
}
\description{
\code{check_positivity()} is the general entry point to positively. It detects the
exposure type, runs the diagnostics that apply to that type with their
defaults, and returns a \link{positivity_check} container whose print method shows
each diagnostic's summary in its own section. Like every positively function,
it is diagnostic only: it reports numbers, not verdicts.
}
\details{
\code{check_positivity()} resolves the exposure type a single time and announces it
once through an informational message (suppressed by
\code{options(positively.quiet = TRUE)}). \code{\link[=check_edp]{check_edp()}} and \code{\link[=check_port]{check_port()}} receive
the resolved type directly; \code{\link[=check_hat_values]{check_hat_values()}}, \code{\link[=check_hdr]{check_hdr()}}, and
\code{\link[=check_extrapolation]{check_extrapolation()}} re-detect it internally, but their repeated
announcement is muffled so that the type is reported only once. Every other
informational message a diagnostic emits still reaches you. When \code{exposure_type}
is supplied explicitly, it is checked against the detected type up front and a
genuine disagreement aborts before any diagnostic runs. The default diagnostic
set depends on the exposure type:
\itemize{
\item \strong{binary}: \code{\link[=check_edp]{check_edp()}}, \code{\link[=check_port]{check_port()}}, \code{\link[=check_extrapolation]{check_extrapolation()}}
\item \strong{categorical}: \code{\link[=check_edp]{check_edp()}}, \code{\link[=check_port]{check_port()}}
\item \strong{continuous}: \code{\link[=check_edp]{check_edp()}}, \code{\link[=check_port]{check_port()}} (with the exposure
categorized), \code{\link[=check_hat_values]{check_hat_values()}}, \code{\link[=check_hdr]{check_hdr()}}
}

Pass \code{diagnostics} to run an explicit subset instead; the requested order is
honored. Every name must be a diagnostic that \code{check_positivity()} composes
and that applies to the resolved exposure type, otherwise the call aborts with
the valid options listed.

\code{\link[=check_eta_bias]{check_eta_bias()}} and \code{\link[=check_density_ratios]{check_density_ratios()}} are never composed here.
The first needs an outcome and the second needs user-supplied density ratios,
so both are called directly rather than through \code{check_positivity()}.

Per-method options are threaded through \code{args}, a named list of option lists,
for example \code{args = list(port = list(alpha = 0.1))}. Every name in \code{args} must
be a diagnostic that is actually being run.
}
\examples{
set.seed(1)
n <- 150
x1 <- rnorm(n)
x2 <- rnorm(n)
ps <- 0.2 + 0.6 * plogis(0.5 * x1 - 0.5 * x2)
df <- data.frame(exposure = rbinom(n, 1, ps), x1 = x1, x2 = x2)

# The binary default set: edp, port, and extrapolation.
check_positivity(df, exposure, c(x1, x2))

# Run a single diagnostic with a tuned option.
check_positivity(
  df,
  exposure,
  c(x1, x2),
  diagnostics = "port",
  args = list(port = list(alpha = 0.1))
)

}
\seealso{
\code{\link[=check_edp]{check_edp()}}, \code{\link[=check_port]{check_port()}}, \code{\link[=check_hat_values]{check_hat_values()}}, \code{\link[=check_hdr]{check_hdr()}},
and \code{\link[=check_extrapolation]{check_extrapolation()}} for the individual diagnostics, and
\code{\link[=check_eta_bias]{check_eta_bias()}} and \code{\link[=check_density_ratios]{check_density_ratios()}} for the two that
\code{check_positivity()} does not call.
}
