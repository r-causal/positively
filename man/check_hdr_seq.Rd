% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check-hdr.R
\name{check_hdr_seq}
\alias{check_hdr_seq}
\title{Diagnose sequential positivity for continuous exposures with HDR non-overlap}
\usage{
check_hdr_seq(
  .data,
  .exposures,
  .covariates,
  .baseline = NULL,
  mass = 0.95,
  values = NULL,
  density_estimator = hdr_density_normal(),
  lag = Inf,
  exposure_type = c("auto", "continuous")
)
}
\arguments{
\item{.data}{A data frame in wide form, one row per subject.}

\item{.exposures}{An ordered tidyselect of exposure columns, one per time
point.}

\item{.covariates}{A list of tidyselect expressions, one per time point, of
the time-varying covariates. A length-one list is recycled across time
points. Write it as a literal \code{list()} call, for example
\code{list(l0, l1, l2)}, rather than a pre-built list held in a variable.}

\item{.baseline}{A tidyselect of baseline covariates always included in every
conditioning set. Defaults to \code{NULL}.}

\item{mass}{The HDR probability mass, a single value strictly between 0 and 1.
Defaults to \code{0.95}.}

\item{values}{A numeric vector of target exposure values. \code{NULL} (the
default) uses a 100-point grid spanning the pooled exposure range.}

\item{density_estimator}{An \code{hdr_density} conditional-density estimator, built
with \code{\link[=hdr_density_normal]{hdr_density_normal()}} (the default) or \code{\link[=new_hdr_density]{new_hdr_density()}}.}

\item{lag}{The history window: the number of earlier time points whose
covariates and exposures enter each conditioning set. Defaults to \code{Inf}, the
full history.}

\item{exposure_type}{One of \code{"auto"} (detect from the data, the default) or
\code{"continuous"}, applied to every exposure column in turn. A supplied type is
authoritative and detection is not consulted, so \code{"continuous"} is rejected
only when an exposure column is not numeric. Either way the error names
every offending column at once, rather than the first one it meets.
Declaring it on numeric columns with few distinct values, doses recorded at
a handful of milligram levels for instance, is exactly the supported use:
the unique-value heuristic reads such a column as categorical, so under
\code{"auto"} the call aborts. Because numeric is the whole of what the type asks
of a column, a two-valued numeric column is then accepted as well, which is
the price of an authoritative declaration. A constant column is refused
whichever way the type was decided, and the error names every constant
column at once.}
}
\value{
An \code{hdr_result} object, an S7 subclass of \link{positivity_diagnostic}.
Its \verb{@results} tibble has one row per time point and target value with
columns \code{time}, \code{value}, and \code{nonoverlap}.
}
\description{
\code{check_hdr_seq()} applies the HDR non-overlap ratio of Bao and Schomaker
(2025) to a time-varying continuous exposure, one time point at a time. At
each time point it fits the conditional density of that exposure on the
history entering its conditioning set, following the sequential-support logic
of sPoRT (Chatton et al.), and reports the non-overlap ratio at the target
values. It reports a diagnostic only and assigns no severity grade.
}
\details{
Data are supplied in wide form, one row per subject with one exposure column
per time point. For time point \eqn{t} the diagnostic fits the conditional
density of the exposure at \eqn{t} on its conditioning set, then computes the
point-in-time non-overlap ratio \eqn{\hat{\tau}_t(a)} exactly as
\code{\link[=check_hdr]{check_hdr()}} does. The conditioning set is the baseline covariates, the
time-varying covariates for each time point within the \code{lag} history window,
and the exposures at earlier time points within that window. With the default
\code{lag = Inf} the full history enters.

Because each time point is treated separately, a support gap that moves a
stratum's supported dose away from a target is isolated to the time point
where it occurs. The default normal estimator sees mean-shift gaps only; a
multimodal gap at a single time point is invisible to it, as for
\code{\link[=check_hdr]{check_hdr()}}.

A time point whose exposure is close to a deterministic function of its
conditioning set reads a ratio of one throughout that time point's observed
exposure range, for the reason \code{\link[=check_hdr]{check_hdr()}} describes. Each time point is
judged on a dense grid spanning its own range, since the shared target grid
spans the pooled range and can leave a time point with a single target of its
own. One warning names every such time point.
}
\examples{
set.seed(1)
n <- 200
df <- data.frame(
  l0 = rnorm(n),
  a1 = rnorm(n),
  l1 = rnorm(n),
  a2 = rnorm(n),
  l2 = rnorm(n),
  a3 = rnorm(n)
)

result <- check_hdr_seq(
  df,
  c(a1, a2, a3),
  list(l0, l1, l2),
  values = c(-2, 0, 2)
)
result

}
\references{
Bao Y, Schomaker M (2025). Feasible Dose-Response Curves for Continuous
Treatments Under Positivity Violations.

Chatton A, Schomaker M, Luque-Fernandez MA, Platt RW, Schnitzer ME (2025).
Is checking for sequential positivity violations getting you down? Try
sPoRT!
}
