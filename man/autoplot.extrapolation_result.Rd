% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-extrapolation.R
\name{autoplot.extrapolation_result}
\alias{autoplot.extrapolation_result}
\alias{plot.extrapolation_result}
\title{Plot an extrapolation diagnostic}
\arguments{
\item{object}{An \code{extrapolation_result} from \code{\link[=check_extrapolation]{check_extrapolation()}}.}

\item{type}{One of \code{"distribution"} or \code{"hull"}.}

\item{...}{Not used.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Draws one of two views of a \code{\link[=check_extrapolation]{check_extrapolation()}} result.
\code{type = "distribution"} shows the distribution of \code{frac_nearby} as histograms
faceted by exposure group, the share of the opposite group that lies nearby
each observation. \code{type = "hull"} shows convex-hull membership as stacked bars
per exposure group, and is available only when the hull test ran.
}
\examples{
set.seed(1)
n <- 100
x1 <- rnorm(n)
x2 <- rnorm(n)
exposure <- rbinom(n, 1, plogis(0.5 * x1 - 0.5 * x2))
df <- data.frame(exposure = exposure, x1 = x1, x2 = x2)
result <- check_extrapolation(df, exposure, c(x1, x2), hull = FALSE)

ggplot2::autoplot(result, type = "distribution")

}
